#ifndef RUNCALL_HPP_INCLUDED
#define RUNCALL_HPP_INCLUDED

#include "tcp_connection.hpp"

using namespace boost;

void RunConstruct(shared_ptr<tcp_connection> conn, string ns, string cls, CallID_t callid, vector<TypedVal> args);
void RunCallStatic(shared_ptr<tcp_connection> conn, string ns, string cls, string method, CallID_t callid, vector<TypedVal> args);
void RunCallMethod(shared_ptr<tcp_connection> conn, ObjectID_t objectid, CallID_t callid, vector<TypedVal> args);

#endif // RUNCALL_HPP_INCLUDED
